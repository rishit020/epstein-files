---
phase: 06-video-output-wiring
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_webcam_source.py
  - tests/unit/test_audio_handler.py
  - tests/unit/test_event_logger.py
  - tests/integration/test_pipeline_wiring.py
autonomous: true
requirements:
  - FR-0.1
  - FR-0.2
  - FR-0.3
  - FR-0.4
  - FR-6.1
  - FR-6.3
  - PRD-9
  - PRD-3.3
must_haves:
  truths:
    - "All 4 test files exist and are importable by pytest"
    - "Each test file contains at least the named test functions that will fail (RED phase of TDD)"
    - "pytest discovers all test files without import errors"
  artifacts:
    - path: "tests/unit/test_webcam_source.py"
      provides: "WebcamSource unit test stubs"
      contains: "class TestWebcamSource"
    - path: "tests/unit/test_audio_handler.py"
      provides: "AudioAlertHandler unit test stubs"
      contains: "class TestAudioAlertHandler"
    - path: "tests/unit/test_event_logger.py"
      provides: "EventLogger unit test stubs"
      contains: "class TestEventLogger"
    - path: "tests/integration/test_pipeline_wiring.py"
      provides: "Pipeline integration test stubs"
      contains: "class TestPipelineWiring"
  key_links:
    - from: "tests/unit/test_webcam_source.py"
      to: "layer0_video/webcam_source.py"
      via: "import"
      pattern: "from layer0_video.webcam_source import WebcamSource"
    - from: "tests/unit/test_audio_handler.py"
      to: "layer6_output/audio_handler.py"
      via: "import"
      pattern: "from layer6_output.audio_handler import AudioAlertHandler"
    - from: "tests/unit/test_event_logger.py"
      to: "layer6_output/event_logger.py"
      via: "import"
      pattern: "from layer6_output.event_logger import EventLogger"
---

<objective>
Create all test stub files for Phase 6, establishing the RED phase of TDD. Every test function
should be named and structured to test a specific PRD requirement. Tests will fail with ImportError
or NotImplementedError until the corresponding implementation plans (02-05) are executed.

Purpose: Wave 0 ensures every subsequent plan has runnable tests from the start. This satisfies
the Nyquist validation strategy (VALIDATION.md Dimension 8) and enables TDD-style development.

Output: 4 test files with named test functions covering all Phase 6 PRD requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-video-output-wiring/06-CONTEXT.md
@.planning/phases/06-video-output-wiring/06-RESEARCH.md
@.planning/phases/06-video-output-wiring/06-VALIDATION.md
@layer0_video/messages.py
@layer1_perception/messages.py
@layer5_alert/messages.py
@layer5_alert/alert_types.py
@layer3_temporal/messages.py
@config.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From layer0_video/messages.py:
```python
@dataclass
class RawFrame:
    timestamp_ns: int
    frame_id: int
    width: int
    height: int
    channels: int
    data: np.ndarray      # Shape: (height, width, 3), dtype: uint8, BGR
    source_type: str      # 'imx219_v4l2' | 'webcam' | 'file'
```

From layer5_alert/messages.py:
```python
@dataclass
class AlertCommand:
    alert_id: str           # UUID
    timestamp_ns: int
    level: AlertLevel       # AlertLevel.HIGH or AlertLevel.URGENT
    alert_type: AlertType   # AlertType.VISUAL_INATTENTION, .PHONE_USE, etc.
    composite_score: float
    suppress_until_ns: int
```

From layer5_alert/alert_types.py:
```python
class AlertLevel(Enum):
    LOW = 1; HIGH = 2; URGENT = 3

class AlertType(Enum):
    VISUAL_INATTENTION = 'D-A'
    HEAD_INATTENTION = 'D-B'
    DROWSINESS = 'D-C'
    PHONE_USE = 'D-D'
    FACE_ABSENT = 'FACE'
```

From layer3_temporal/messages.py:
```python
@dataclass
class TemporalFeatures:
    timestamp_ns: int
    gaze_off_road_fraction: float
    gaze_continuous_secs: float
    head_deviation_mean_deg: float
    head_continuous_secs: float
    perclos: float
    blink_rate_score: float
    phone_confidence_mean: float
    phone_continuous_secs: float
    speed_zone: str
    speed_modifier: float
    frames_valid_in_window: int
    face_absent_continuous_secs: float = 0.0
    thermal_throttle_active: bool = False
```

From config.py (relevant constants):
```python
CAPTURE_WIDTH = 1280
CAPTURE_HEIGHT = 720
CAPTURE_FPS = 30
FRAME_QUEUE_DEPTH = 2
PHONE_THREAD_TIMEOUT_MS = 5
AUDIO_ALERT_SOUND = '/System/Library/Sounds/Ping.aiff'
AUDIO_ALERT_SOUND_URGENT = '/System/Library/Sounds/Sosumi.aiff'
LOG_DIR = 'logs/'
LOG_MAX_BYTES = 52_428_800
LOG_BACKUP_COUNT = 5
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs for WebcamSource, AudioAlertHandler, and EventLogger</name>
  <files>
    tests/unit/test_webcam_source.py
    tests/unit/test_audio_handler.py
    tests/unit/test_event_logger.py
  </files>
  <read_first>
    tests/unit/test_perception_stack.py (reference for test patterns: sys.path, imports, mock factories, naming)
    layer0_video/messages.py (RawFrame dataclass)
    layer5_alert/messages.py (AlertCommand dataclass)
    layer5_alert/alert_types.py (AlertLevel, AlertType enums)
    layer3_temporal/messages.py (TemporalFeatures dataclass)
    config.py (all constants referenced in tests)
    .planning/phases/06-video-output-wiring/06-CONTEXT.md (D-02, D-03, D-04 for exact specifications)
  </read_first>
  <action>
Create three unit test files following the existing pattern from `test_perception_stack.py`:

**tests/unit/test_webcam_source.py:**
- Add sys.path insert at top (same as other test files: `sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..'))`)
- Import: `from layer0_video.webcam_source import WebcamSource, SourceUnavailableError`
- Use `unittest.mock.patch('cv2.VideoCapture')` to mock the camera
- Test class `TestWebcamSource` with these test functions:
  - `test_init_opens_device_index_zero` — mock VideoCapture(0), assert cap.isOpened() called
  - `test_init_sets_resolution` — assert cap.set called with CAP_PROP_FRAME_WIDTH=1280, CAP_PROP_FRAME_HEIGHT=720, CAP_PROP_FPS=30
  - `test_init_raises_source_unavailable_on_timeout` — mock isOpened() returning False, assert SourceUnavailableError raised within 5s (use monkeypatch on time.monotonic to skip real wait)
  - `test_read_returns_raw_frame` — mock cap.read() returning (True, np.zeros((720,1280,3),uint8)), assert return is RawFrame with source_type='webcam', frame_id=0, width=1280, height=720, channels=3
  - `test_read_monotonic_frame_id` — call read() 3 times, assert frame_id is 0, 1, 2
  - `test_read_returns_none_on_failure` — mock cap.read() returning (False, None), assert returns None
  - `test_release_calls_cap_release` — assert cap.release() called
  - `test_read_timestamp_uses_monotonic_ns` — assert timestamp_ns field is int and > 0

Each test body should contain `pytest.skip("Stub — implementation in Plan 02")` as first line so they are discoverable but do not fail with ImportError. The import line at the top should be wrapped in a try/except ImportError that sets a module-level `_IMPL_MISSING = True` flag, and a `pytestmark = pytest.mark.skipif(_IMPL_MISSING, reason="WebcamSource not yet implemented")` at module level.

**tests/unit/test_audio_handler.py:**
- Import: `from layer6_output.audio_handler import AudioAlertHandler`
- Import: `from layer5_alert.alert_types import AlertLevel, AlertType` and `from layer5_alert.messages import AlertCommand`
- Helper factory: `_make_alert(level=AlertLevel.HIGH, alert_type=AlertType.VISUAL_INATTENTION)` returning an AlertCommand with uuid alert_id, timestamp_ns=1000, composite_score=0.7, suppress_until_ns=2000
- Test class `TestAudioAlertHandler`:
  - `test_play_high_calls_afplay_with_ping` — mock subprocess.Popen, call play(alert_high), assert Popen called with ['afplay', '/System/Library/Sounds/Ping.aiff']
  - `test_play_urgent_calls_afplay_with_sosumi` — mock subprocess.Popen, call play(alert_urgent), assert Popen called with ['afplay', '/System/Library/Sounds/Sosumi.aiff']
  - `test_play_does_not_wait_on_process` — mock subprocess.Popen, assert .wait() and .communicate() NOT called
  - `test_play_logs_warning_on_slow_dispatch` — mock subprocess.Popen to take >50ms (monkeypatch time.perf_counter), assert logging.warning called with "50ms" in message
  - `test_play_catches_exceptions` — mock subprocess.Popen to raise OSError, assert no exception propagates

Same _IMPL_MISSING / skipif pattern as webcam tests.

**tests/unit/test_event_logger.py:**
- Import: `from layer6_output.event_logger import EventLogger`
- Import: AlertCommand, AlertLevel, AlertType, TemporalFeatures
- Use `tmp_path` pytest fixture for log directory
- Test class `TestEventLogger`:
  - `test_log_alert_writes_jsonl` — create EventLogger(log_dir=tmp_path), call log_alert with AlertCommand and TemporalFeatures, read file, assert line is valid JSON with "event_type": "ALERT"
  - `test_log_alert_contains_required_fields` — verify JSON has keys: event_type, timestamp_ns, alert_id, alert_type, alert_level, composite_score, active_classes, gaze_continuous_secs, head_continuous_secs, perclos, phone_confidence, suppress_until_ns
  - `test_log_state_transition_writes_jsonl` — call log_state_transition("NOMINAL", "ALERTING", "ALT-01", 100, 1000), assert JSON has event_type="STATE_TRANSITION", previous_state, new_state, trigger, frame_id
  - `test_log_degraded_writes_jsonl` — call log_degraded("perception_invalid_60_frames", 2.0, 1000), assert JSON has event_type="DEGRADED", reason, duration_secs
  - `test_log_watchdog_timeout_writes_jsonl` — call log_watchdog_timeout(10531, 2.14, 1000), assert JSON has event_type="WATCHDOG_TIMEOUT", last_frame_id, secs_since_last_frame
  - `test_log_calibration_complete_writes_jsonl` — call log_calibration_complete({...}), assert JSON has event_type="CALIBRATION_COMPLETE"
  - `test_enum_serialization_uses_value` — log an alert, verify alert_type is "D-A" not "AlertType.VISUAL_INATTENTION", alert_level is "HIGH" not "AlertLevel.HIGH"
  - `test_rotating_file_handler_configured` — monkeypatch config.LOG_MAX_BYTES=100, write enough to trigger rotation, assert backup file exists

Same _IMPL_MISSING / skipif pattern.
  </action>
  <verify>
    <automated>cd /Users/rishits/Documents/attentia_v2 && python3 -m pytest tests/unit/test_webcam_source.py tests/unit/test_audio_handler.py tests/unit/test_event_logger.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_webcam_source.py contains "class TestWebcamSource" and at least 8 test functions matching "def test_"
    - tests/unit/test_audio_handler.py contains "class TestAudioAlertHandler" and at least 5 test functions matching "def test_"
    - tests/unit/test_event_logger.py contains "class TestEventLogger" and at least 8 test functions matching "def test_"
    - All three files contain "_IMPL_MISSING" skipif guard
    - `python3 -m pytest tests/unit/test_webcam_source.py tests/unit/test_audio_handler.py tests/unit/test_event_logger.py --collect-only -q` exits 0 and shows collected test count
    - No test file imports anything from layer0_video.webcam_source or layer6_output at module level without try/except ImportError guard
  </acceptance_criteria>
  <done>All 3 unit test stub files exist, are collected by pytest without errors, and skip gracefully until implementations exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test stubs for pipeline wiring</name>
  <files>
    tests/integration/test_pipeline_wiring.py
  </files>
  <read_first>
    tests/unit/test_perception_stack.py (reference for mock patterns)
    layer0_video/messages.py (RawFrame)
    layer1_perception/messages.py (PerceptionBundle, PhoneDetectionOutput)
    layer1_perception/perception_stack.py (PerceptionStack constructor signature)
    layer1_perception/phone_detector.py (PhoneDetector.infer signature)
    .planning/phases/06-video-output-wiring/06-CONTEXT.md (D-01 thread arch, D-05 lifecycle)
    .planning/phases/06-video-output-wiring/06-RESEARCH.md (Pattern 2: T-1/T-2 merge, Pattern 7: mock source)
  </read_first>
  <action>
Create `tests/integration/test_pipeline_wiring.py`:

- sys.path insert at top (same pattern as unit tests)
- Guarded import of main module: `try: from main import build_pipeline, run_pipeline ... except ImportError: _IMPL_MISSING = True`
- Also import: RawFrame, PerceptionBundle, PhoneDetectionOutput, AlertCommand, AlertLevel, AlertType
- Module-level skipif guard

Helper factories:
- `_make_synthetic_frame(frame_id: int) -> RawFrame` — returns RawFrame with timestamp_ns=frame_id*33_333_333 (30fps spacing), width=640, height=480, channels=3, data=np.zeros((480,640,3), uint8), source_type='webcam'
- `_make_mock_perception_stack()` — returns a MagicMock whose .infer() returns a safe PerceptionBundle with face.present=False
- `_make_mock_phone_detector()` — returns a MagicMock whose .infer() returns PhoneDetectionOutput(detected=False, max_confidence=0.0, bbox_norm=None)

Test class `TestPipelineWiring`:
  - `test_t0_puts_frame_into_both_queues` — create 2 queues, simulate T-0 put logic, assert both queues receive the frame
  - `test_t1_produces_perception_bundle` — feed a RawFrame to mock PerceptionStack.infer(), verify PerceptionBundle returned
  - `test_t2_produces_phone_detection` — feed a RawFrame to mock PhoneDetector.infer(), verify PhoneDetectionOutput returned
  - `test_t1_t2_merge_uses_phone_result_when_available` — simulate T-1 completing, T-2 result available, verify merged bundle has phone_result_stale=False
  - `test_t1_t2_merge_marks_stale_on_timeout` — simulate T-1 completing, T-2 result NOT available within 5ms, verify merged bundle has phone_result_stale=True
  - `test_t3_runs_full_pipeline_chain` — feed a PerceptionBundle through SignalProcessor->TemporalEngine->ScoringEngine->AlertStateMachine (all real, not mocked), verify DistractionScore or AlertCommand|None returned
  - `test_stop_event_terminates_all_threads` — start threads, set stop_event, join with timeout=2s, assert all threads are dead
  - `test_shutdown_joins_all_threads` — start threads with mock source that produces 5 frames then stops, assert threads exit cleanly
  - `test_lstm_hidden_state_persists_across_frames` — feed 2 frames through T-1 mock, verify second call to PerceptionStack.infer() receives hidden_state from first call's bundle

Each test body: `pytest.skip("Stub — implementation in Plan 05")`
  </action>
  <verify>
    <automated>cd /Users/rishits/Documents/attentia_v2 && python3 -m pytest tests/integration/test_pipeline_wiring.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_pipeline_wiring.py contains "class TestPipelineWiring" and at least 9 test functions matching "def test_"
    - File contains "_IMPL_MISSING" skipif guard
    - `python3 -m pytest tests/integration/test_pipeline_wiring.py --collect-only -q` exits 0 and shows collected test count
    - File contains helper factories `_make_synthetic_frame`, `_make_mock_perception_stack`, `_make_mock_phone_detector`
  </acceptance_criteria>
  <done>Integration test stub file exists, is collected by pytest without errors, and skips gracefully until main.py is implemented.</done>
</task>

</tasks>

<verification>
All 4 test files collected by pytest:
```bash
python3 -m pytest tests/unit/test_webcam_source.py tests/unit/test_audio_handler.py tests/unit/test_event_logger.py tests/integration/test_pipeline_wiring.py --collect-only -q
```
Should report 30+ collected tests, all skipped.
</verification>

<success_criteria>
- 4 test files exist in their expected paths
- pytest --collect-only finds all test functions without import errors
- Running the tests shows all as SKIPPED (not FAILED or ERROR)
- Test names clearly map to PRD requirements (FR-0.x, FR-6.1, FR-6.3, section 9, section 3.3)
</success_criteria>

<output>
After completion, create `.planning/phases/06-video-output-wiring/06-01-SUMMARY.md`
</output>
